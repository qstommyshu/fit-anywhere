import 'package:flutter/material.dart';

class diet_recom extends StatefulWidget {
  diet_recom({Key? key}) : super(key: key);

  @override
  _diet_recomState createState() => _diet_recomState();
}

class _diet_recomState extends State<diet_recom> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Diet Recommandation"),
      ),
      body: Stack(
        children: [],
      ),
    );
  }
}
